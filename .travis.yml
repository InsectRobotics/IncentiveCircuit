dist: focal

language: python

python:
  - "3.8"

install:
  - pip install -r requirements.txt
  - pip install .

script:
  - cd examples
  - python run_twinspokemodel.py --nb-kc 10 --only-nids --structure